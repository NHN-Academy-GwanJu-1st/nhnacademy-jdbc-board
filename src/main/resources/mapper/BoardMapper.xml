<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.nhnacademy.mapper.BoardMapper">

    <select id="exist" resultType="int">
        SELECT COUNT(*)
        FROM Board
        WHERE id = #{id}
    </select>

    <select id="findAll" resultType="com.nhnacademy.domain.Board">
        SELECT * FROM Board
    </select>

    <select id="findById" resultType="com.nhnacademy.domain.Board">
        SELECT *
        FROM Board
        WHERE id = #{id}
    </select>

    <insert id="registerBoard">
        INSERT INTO Board (userId, title, content)
        VALUES (#{userId}, #{title}, #{content})
    </insert>

    <update id="modifyBoard">
        UPDATE Board
        SET modifierId = #{modifierId}, title = #{title}, content = #{content}, updateDate = #{updateDate}
        WHERE id = #{boardId}
    </update>

    <delete id="deleteBoard">
        DELETE FROM Board
        WHERE id = #{id}
    </delete>



</mapper>
